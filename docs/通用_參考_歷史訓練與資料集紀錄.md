# 歷史訓練與資料集紀錄

**用途**：把散在各版本報告裡的數字集中成一份可查表，並**明確標示哪些數字之間可以比較**。
**機器可讀版**：[training_history.json](training_history.json)（由各版本的原始輸出直接讀出，非手抄）
**維護方式**：每完成一次訓練就補一列。數字一律從 `results.csv` / `summary.json` /
`overview_metrics.json` 讀，不從報告內文轉抄。

> **這份文件只放數字與可比性判斷**，不放結論與推導——那些在各版本自己的報告裡。

---

## 1. 資料集

| 版本 | 類別 | train / valid / test（影像） | 框數 | 跨 split 近重複 | 建置腳本 |
| --- | ---: | --- | --- | --- | --- |
| `v5` | 8 | 7,140 / 436 / 445 | 未記錄 | 未查驗（方法論同 v5r，大機率有） | — |
| `v5r` | 8 | 7,145 / 438 / 438 | 未記錄 | **三個 Pests 類共 13.4%**（未修） | `build_dataset_v5r.py` |
| `v5.5` | 9 | 7,544 / 327 / 326 | 18,975 / 729 / 689 | **全類別 0 組** | `build_dataset_v5_5.py` |
| **`v5.6`** | 9 | **7,264 / 401 / 401** | **18,078 / 868 / 822** | **全類別 0 組** | `build_dataset_v5_6.py` |

**每一版改了什麼**

| 版本 | 改動 |
| --- | --- |
| `v5r` | Thrips 兩子類合併（極差 24.7x→**9.1x**）；Aphid 丟棄 `<20px` 框（26.0x→**9.4x**）；檔名帶類別前綴 |
| `v5.5` | 新增 `Thrips_Damage`（第 9 類）；`Scale_Insect` 與 `Canker` 全面重標；去洩漏改為「近重複群組整群移進 train，不刪圖」 |
| `v5.6` | 切分改逐類絕對評估量、與 v5.5 **巢狀相容**；修掉全類別的框膨脹缺陷（旋轉只用 90° 倍數、邊界改 `REPLICATE`）；逐類增強 profile |

> `v5` 那一列的框數是 null，因為 `archive/v5_說明_資料集建置技術摘要.md` 把數字嵌成圖片，
> 無法解析。影像數引自 `archive/v5r_說明` §7 的對照括號。

---

## 2. 訓練

| 版本 | 資料集 | 類別 | 排程 | 實跑 | 最佳輪次 | 報告用的權重 | 每輪 |
| --- | --- | ---: | --- | ---: | ---: | --- | ---: |
| `v8` | v5 | 8 | 160，patience 30 | 99 | ep69 | `best.pt` | — |
| `v9` 六臂 | v5r | 8 | 60，無早停 | 60 | — | 平台期平均 | — |
| `v9/A0` 長跑 | v5r | 8 | 160，無早停 | 160 | ep69 | `best.pt` | — |
| `v10` | v5.5 | 9 | 100，patience 30 | 84 | ep54 | `best.pt` | — |
| `v11` | v5.6 | 9 | 100，patience 30 | 99 | ep69 | `best.pt` | 161 s |
| **`v11.5`** | v5.6 | 9 | **70，patience 0** | 70 | ep53 | **`last.pt`** | 157 s |

### 指標

| 版本 | valid mAP50 | valid mAP50-95 | test mAP50 | test mAP50-95 | test Jaccard |
| --- | ---: | ---: | ---: | ---: | ---: |
| `v8` | 0.82082 | 0.64688 | — | — | — |
| `v9/A0` | 0.86651 | 0.65356 | **0.89079** | **0.67829** | 0.66979 |
| `v10` | 0.79931 | 0.59268 | 0.80928 | 0.60605 | 0.71906 |
| `v11` | 0.83543 | 0.62924 | 0.82213 | 0.61294 | — |
| **`v11.5`** | 0.82280 | 0.62970 | 0.80950 | 0.60650 | 0.68700 |

> `v8` 沒有獨立的 test 評估。`v11` 的 Jaccard 未算（當時只存了 `last.pt @ test` 的混淆矩陣）。

### v9 六臂消融（平台期 mAP50-95，60 輪）

| 臂 | 改動 | mAP50-95 | Δ vs A0 |
| --- | --- | ---: | ---: |
| **A0** | 基準 | **0.63399 ± 0.00383** | — |
| A4 | imgsz 800 | 0.63652 | +0.00253（+0.7σ） |
| A2 | ADown ×6 | 0.62657 | −0.00742（−1.9σ） |
| A1 | W-I-MPDIoU r=0.70 | 0.61938 | −0.01461（−3.8σ） |
| A1b | W-I-MPDIoU r=1.25 | 0.61562 | −0.01837（−4.8σ） |
| A3 | StarTripletBlock ×3 | 0.60789 | −0.02610（−6.8σ） |

**2σ 門檻 0.64165，零臂通過。**

---

## 3. 可比性矩陣（最重要的一節）

| 想比較 | 可以嗎 | 原因 |
| --- | :-: | --- |
| `v8` ↔ 之後任何一版 | **✗** | v5r 的 valid 有 44.5%、test 有 47.5% 落在 v5 的 train 裡，**不存在對 v8 乾淨的評估集**；標註約定也改過 |
| `v9/A0` ↔ `v10`/`v11`/`v11.5` | **✗** | 8 類 vs 9 類；v5r 有 13.4% 洩漏而 v5.5/v5.6 是 0%；`Scale_Insect` 已重標成不同的任務 |
| `v10` ↔ `v11` ↔ `v11.5`（各自的完整評估集） | **✗** | v5.5 是 327/326、v5.6 是 401/401，評估集不同 |
| **`v10` ↔ `v11` ↔ `v11.5`（v5.5 子集）** | **✓** | v5.6 與 v5.5 **巢狀相容**，可取出完全同一批 327/326 張影像 |
| 六臂之間 | ✓ | 同資料集、同排程，只差一項改動 |

### 唯一乾淨的三版對照（v5.5 的 326 張 test）

| 版本 | 權重 | mAP50 | mAP50-95 |
| --- | --- | ---: | ---: |
| `v10` | `best.pt` | 0.809 | 0.606 |
| `v11` | `best.pt` | 0.800 | 0.603 |
| `v11.5` | `last.pt` | 0.788 | 0.599 |
| **全距** | | **0.021** | **0.007** |

三版的 train 資料相差不到 6%，**這個全距基本上就是 run 間變異**。
注意 v10/v11 報的是 `best.pt`（帶向上偏誤），v11.5 報的是 `last.pt`。

---

## 4. 已驗證的常數與經驗值

| 項目 | 值 | 出處 |
| --- | --- | --- |
| 標註邊長極差 ↔ per-class AP 的 Spearman ρ | **−0.976** | `archive/v5r_說明` §2 |
| per-class AP50 的雜訊地板 | **±0.04** | v11 報告 §3（資料完全沒動的五類也移動這麼多） |
| run 間全距（test mAP50 / mAP50-95） | **0.021 / 0.007** | v11.5 報告 §4 |
| 最佳輪次 | v8 **69**、v9/A0 **69**、v10 54、v11 **69**、v11.5 53 | 各版 `results.csv` |
| `best.pt` 高出平台期 | v10 **2.8σ**、v11 **2.7σ**、v11.5 **1.5σ** | 各版 `summary.json` |
| `close_mosaic` | **四次**（v9/v10/v11/v11.5）皆確認幫倒忙 | 各版 `results.csv` |
| ultralytics 版本 | **8.4.121（釘死）** | 全部版本 |

---

## 5. 訓練協定的演進

| 期間 | 協定 | 問題 |
| --- | --- | --- |
| v8 – v10 | `patience=30`，報 `best.pt` | `best.pt` 由 valid 挑出，帶選擇偏誤；v10 實測高出平台期 2.8σ |
| v9 六臂 | 無早停，報**平台期平均** | 正確，但要同時解釋兩個數字 |
| v11 | `patience=30`，報 `best.pt` | 同上；且 valid 不可與 test 併計 → 最差 ±2SE 只能到 0.140 |
| **v11.5 起** | **`patience=0` ＋ 固定輪數，報 `last.pt`** | **目前的標準。** valid 未參與任何決策，可與 test 合法併計 |

**為什麼 `last.pt` 可以取代平台期平均**：v11.5 實測 `best` 與 `last` 的差距全部 ≤ 0.004，
且兩個指標在 test 上方向相反——沒有可辨識的差異。
改用 `last.pt` 不用付代價，卻讓 ±2SE 從 0.140 降到 **0.099**。

---

## 6. 評估精度（±2SE）的演進

以各類自己實測的 ±2SE 依 1/√n 投影：

| 類別 | v10（test 25 圖等） | v11（只能報 test） | **v11.5（pooled）** |
| --- | ---: | ---: | ---: |
| Citrus_Leaf_Miner | 0.210 | 0.140 | **0.099** |
| Thrips | 0.170 | 0.137 | **0.097** |
| Thrips_Damage | 0.161 | 0.117 | **0.083** |
| Scale_Insect | 0.082 | 0.069 | **0.049** |
| Aphid | 0.048 | 0.048 | 0.034 |
| Canker | 0.061 | 0.061 | 0.043 |
| Black_Spot | 0.043 | 0.043 | 0.030 |
| Sooty_Mold | 0.030 | 0.030 | 0.021 |
| Oily_Spot | 0.022 | 0.022 | 0.016 |
| **最差** | **0.210** | **0.140** | **0.099 ✓** |

**目標 ≤ 0.10 於 v11.5 達成**，而且沒有新拍任何一張照片。

### valid/test 矛盾的收斂

| 類別 | v10 | v11 | v11.5 | 門檻 |
| --- | ---: | ---: | ---: | ---: |
| Scale_Insect | 0.308 ✗ | 0.096 | **0.040** ✓ | 0.098 |
| Thrips_Damage | 0.285 ✗ | 0.190 ✗ | **0.202 ✗** | 0.165 |
| Citrus_Leaf_Miner | 0.137 | 0.078 | **0.039** ✓ | 0.198 |
| Thrips | 0.076 | 0.071 | **0.079** ✓ | 0.194 |

`Thrips_Damage` 是唯一沒有隨評估集擴大而收斂的——成因是框定義不一致，不是樣本量。

---

## 7. 交付物位置

| 版本 | 位置 |
| --- | --- |
| `v8` | `Train Records/YOLO26n_P2_Citrus_MuSGD_v8/` |
| `v9` | `Train Code/v9/Train_output/Phase4/`（含交付權重 `best.pt` @ ep69） |
| `v10` | `Train Code/v10/Train_output/extracted/` |
| `v11` | `Train Code/v11/Train Output/extracted/` |
| `v11.5` | `Train Code/v11.5/Train Output/extracted/`（主要權重 `last.pt`） |

> 各版的 `extracted/` 只保留 `best.pt` / `last.pt` / `args.yaml` / JSON / CSV，
> 其餘由同目錄的 zip 完整重現（`.gitignore` 規則見專案根目錄）。
