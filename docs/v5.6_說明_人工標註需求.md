# v5.6 說明：人工標註需求（技術規格）

**日期**：2026-09-05
**對象**：專案內部。**組員看的是另一份**——
`Datasets/1_原始影像/_人工標註待辦/{A,B,C}/說明書.md`，
白話、零術語、六段固定結構、附正例反例對照圖。
兩份**不是同一份的複製**，讀者不同，內容就不同。

---

## 0. 三個工作包

| 包 | 產出的東西 | 用在哪 | 人力 |
| --- | --- | --- | --- |
| **A** 薊馬葉害_框怎麼畫 | **一個決策**，不是資料 | 決定 `Thrips_Damage` 要重標還是刪除（v5.7） | 2 人 × 30 分 |
| **B** 潛葉蛾_補標150張 | 150 張 YOLO 標註 | `build_dataset_v5_6.py --arm ext` 的輸入 | 1 人 × 1.5 時 |
| **C** 合成圖_看圖打勾 | 一張通過/不通過清單 | `--arm cp` 的放行閘 | 1 人 × 15 分 |

工作區：`Datasets/1_原始影像/_人工標註待辦/`（`Datasets/` 未進版控）。
產生工具：`tools/prepare_manual_packages.py`（待處理影像）、
`tools/make_annotation_guide.py`（正例反例對照圖）、
`tools/copy_paste_clm.py --qa`（C 的待檢查影像）。

---

## 1. 類別 id 的處理（最容易出錯的一步，已從流程中移除）

**組員的標註軟體裡只會有一個類別**，存出來的 `.txt` 首欄一律是 `0`。
真正的 id 由建置腳本重映：

| 包 | 組員看到的類別名 | 存出來的 id | 建置時映射成 |
| --- | --- | ---: | ---: |
| A | `thrips_damage` | 0 | 8（`Thrips_Damage`） |
| B | `leafminer` | 0 | **5**（`Citrus_Leaf_Miner`），由 `EXT_CID = 5` 指定 |

**理由**：要組員在 LabelImg 裡自己建九個類別、還要順序完全正確，
是整條流程裡最容易靜默出錯的一步——順序錯了不會報錯，
只會讓整批標註掛到錯的類別上，而且很難事後發現。
單類別 + 程式重映把這個風險降到零。

`build_dataset_v5_6.py:collect()` 的 `cid` 參數就是為此存在：
呼叫時傳入 `cid=EXT_CID`，忽略檔案裡的原始首欄值。

---

## 2. A · 薊馬葉害的標註約定

### 2.1 這個包量的是什麼

`Thrips_Damage` 在 v10 的 medIoU 是 0.725–0.727、**R@0.75 只有 0.290–0.375**——
模型找得到病徵，但框畫不準。這通常代表**既有的框本身就不一致**。

20 張的雙人一致性測試回答：**這個類別的邊界能不能被一致定義？**

| 兩人中位 IoU | 結論 | 下一步 |
| --- | --- | --- |
| **≥ 0.85** | 約定可行 | 值得投入全類 208 張重標（v5.7 範圍，不在 v5.6） |
| **< 0.85** | 邊界本質上定不出來 | 依 [v11 §7 中止條件](v11_計畫_資料擴充與切分重新設計.md) **刪除該類別**（[P3 §2.2](archive/v9_說明_P3定位精度改善與測試流程.md) 已分析刪除代價接近零） |

評分：`tools/score_annotation_agreement.py 甲的資料夾 乙的資料夾`。
貪婪最大 IoU 配對，**落單的框以 IoU = 0 計入**——漏標也是不一致，
不能只算兩邊都畫到的部分。輸出除了數字還會印白話結論與後續建議。

### 2.2 書面約定的三條規則

1. 框貼著食痕最外緣，不留白邊也不切邊
2. 一塊連續的食痕 = 一個框
3. **兩塊食痕中間夾的健康葉肉，比兩塊裡較小的那一塊還寬 → 分成兩個框**

> **規則 3 與 [P3 §2.4](archive/v9_說明_P3定位精度改善與測試流程.md) 的差異，是刻意的。**
> P3 建議的是「間隔超過 20px 就分框」。絕對像素值對**程式**是好規格，
> 對**人**不是——標註者的畫面縮放倍率隨時在變，沒有人在標註時去量 20px。
> 改成「和較小那塊食痕比寬度」是尺度不變的，用眼睛比一下就能判斷，
> 不同的人、不同的縮放倍率會得到一樣的答案。這正是這個包要測的東西。

### 2.3 選圖

`prepare_manual_packages.py:prepare_a()`——取自 `Thrips_Damage` 的 **train**
（不碰評估集），依框面積排序後等距取樣 20 張，
確保涵蓋不同受害程度而不是全部同一種難度。
原始檔名記在 `_原始檔名對照.csv`。

---

## 3. B · 外部潛葉蛾試點

### 3.1 為什麼只有 150 張，以及為什麼一定是獨立實驗臂

網路搜尋與本地 `craft_datasets_undone` 的完整結論見
[v5.6_計畫_無新拍攝的資料強化.md](v5.6_計畫_無新拍攝的資料強化.md) §2.4。摘要：

- **找不到任何附框的柑橘潛葉蛾偵測資料集**，全部要從零標
- 本地五個外部資料集**全部零標註檔**，且都是離體單葉／白底或去背
- 我們的部署域是 2048×1536 田間手持照（有土壤、雜草、自然陰影、入鏡的手指）

**同一個類別裡混入不同域，正是 [P3 §1.4](archive/v9_說明_P3定位精度改善與測試流程.md)
記載的 Thrips 框定義問題的成因。** 所以：

1. 外部影像**只能進 train**，永遠不進評估集（Gate 3 硬性檢查）
2. 必須當**獨立實驗臂**（`--arm ext`），不能悄悄併進 base
3. 規模刻意壓小到 150 張——先用最小的代價回答「有沒有用」，
   而不是先投入 953 張的標註工時再來後悔

### 3.2 選圖

來源：`craft_datasets_undone/.../Large-Scale Lemon Leaf Disease and Pest Image Data/Citrus_Pest`
（542 張、1440×1080、食痕最清楚的一組）。

`prepare_manual_packages.py:prepare_b()` 先用 dHash（閾值 6，與洩漏查驗同一套）
分群去重：**542 張 → 333 個獨立場景**，每群只留一張代表，再隨機抽 150。

> 不去重直接抽 150，可能抽到大量同一場景的重複影像——
> 那等於用 150 張的標註工時換到遠少於 150 個場景。

### 3.3 併入方式：配額中性

外部影像**不增加 train 總張數**，而是佔用 `Citrus_Leaf_Miner` 既有的
`min(4×raw, 1200)` 增強配額：

```
base :  111 raw +   0 外部 + 333 增強 = 444
ext  :  111 raw + 150 外部 + 183 增強 = 444
```

兩臂張數相同、只有合成組成不同，所以 base vs ext 是乾淨的 A/B，
指標差異可以歸因到「外部資料」而不是「訓練集變大」。

---

## 4. C · 合成影像視覺驗收

### 4.1 為什麼需要人

`--arm cp` 用 `cv2.seamlessClone(MIXED_CLONE)` 把 CLM 食痕融進 train 的
健康株影像。`MIXED_CLONE` 在來源與目標的梯度之間取較強者，
所以會保留食痕紋理、吸收目標葉面的光照——**代價是低對比的食痕可能被洗淡**。

「接縫看不看得出來」「食痕還清不清楚」這兩件事**沒有數值判準**，只能看圖。
所以這是流程裡唯一無法自動化的一道閘。

### 4.2 判準

30 張，每張問三個問題（接縫／食痕清晰度／框準不準），三題全過才算通過。

- **≥ 28/30** → `--arm cp` 放行
- **≤ 27/30** → 不放行，回頭調 `copy_paste_clm.py` 的參數再產一批

說明書裡明寫「不要為了湊到 28 張而放寬標準」——
這道閘的用意就是攔下不好的，少於 28 是有用的結果。

### 4.3 產生方式

`tools/copy_paste_clm.py --qa 30` **獨立跑**，只產 30 張帶編號與標註框的合成圖，
**不建整份資料集**。一份 `OutPut` 約 8.5 GB，為了看 30 張圖而建一份不划算。

---

## 5. 回收機制

`tools/check_annotation_return.py` 是收件端，兩個用途：

```bash
# 組員先交 2 張時的格式預檢（五分鐘內回覆）
.venv/Scripts/python.exe tools/check_annotation_return.py B --precheck

# 全部回收後的正式驗收；B 加 --install 會併進來源樹（id 0 → 5）
.venv/Scripts/python.exe tools/check_annotation_return.py B --install
```

檢查項目：檔案成對、YOLO 格式（會辨認出誤存成 PascalVOC/JSON）、
類別編號是否全為 0、座標是否在 [0,1] 且框不超出照片。
**錯誤訊息刻意寫成可以直接轉貼給組員的白話**，不需要再翻譯一次。

**「先交 2 張」是流程裡最重要的一道防呆。** 最常見的失誤是輸出格式選錯，
而那要等到打開檔案才看得出來——150 張做完才發現，就是白做一個半小時。
兩份說明書的第 3 步都已改成「先畫 2 張就停下來」，檢查表也有對應的勾選項。

---

## 6. 回收之後

| 包 | 回收後要做的事 |
| --- | --- |
| A | `score_annotation_agreement.py` → 依中位 IoU 決定重標或刪除（v5.7） |
| B | 標註放進 `Pests/Citrus_Leaf_Miner_ext.yolo26/train/{images,labels}` → `--arm ext` 重建 → 重跑八道 Gate |
| C | ≥28/30 → `--arm cp` 重建 → 重跑八道 Gate（含視覺抽驗紀錄） |

`--arm cp` 與 `--arm ext` 各會再吃約 8.5 GB 磁碟，建議評估完就刪掉變體，
只留 `_provenance.json` 與指標。

---

## 7. 2026-09-11 流程變更：B 改為「AI 預標 → 單人審圖」

使用者提議：由 AI 代理先粗標，再由使用者一人逐張審圖細修，
取代原本「發給組員從零畫 ＋ 先交 2 張」的流程。一人審圖也讓框的定義只有一套。

### 7.1 同日查到的問題

| 問題 | 事實 | 處理 |
| --- | --- | --- |
| **影像帶 EXIF 旋轉標記** | B 150 張中 **118 張（79%）**、A 20 張中 5 張。`prepare_manual_packages.py` 用 `shutil.copy2` 原樣複製；標註軟體不一定照標記轉、訓練端會轉 → 框可能差 90°，而 `check_annotation_return.py` 的格式檢查抓不到 | **B 已轉正**（寫進像素、拿掉標記；原圖可依 `_原始檔名對照.csv` 從外部資料集取回）。**A 尚未處理**——A 若兩人用不同軟體，這 5 張會讓 IoU 虛低、誤判成刪類別 |
| 現行模型認不出這批影像 | v11.5 在 150 張只有 7 張找到 CLM（5%），121 張零框 | 不能用模型預標 |
| 色彩門檻法不堪用 | 與試點框的中位 IoU 0.42；在 001／002／009／090／120 產生大面積誤框，003／025 漏掉明顯的軌跡 | 不採用 |
| dHash 去重漏掉連拍 | 001–003、004–006 各是同一片葉（相機移動過，閾值 6 抓不到） | 不影響訓練（外部影像只進 train），但 B 的實際多樣性低於 150 張 |

### 7.2 新流程

1. 執行代理依 `B_潛葉蛾_補標150張/AGENTS.md` 粗標，座標記在
   `_預標工作區/boxes.csv`（每列註明由誰標）。001–010 為 Claude 的試點。
   該資料夾自足：`cd` 進去啟動任何代理即可（`GEMINI.md`／`CLAUDE.md` 指向 `AGENTS.md`，
   指令一律走同資料夾的 `prelabel.cmd`）
2. `tools/prelabel_assist.py write` → `預標註/`；`sheet` 讓執行代理自我核對
3. `seed` → `完成後放這裡/`，使用者用 LabelImg 逐張審：**先找漏框**、刪錯框、修邊界
4. `diff` 報告跟預標一模一樣（可能沒審到）的檔案與修改幅度；之後照舊跑
   `check_annotation_return.py B --install`

### 7.3 回收結果（2026-09-12）

**B 包已回收並通過驗收**：150 個標註檔、**185 個框**、26 個空檔、類別編號全部為 0。

審圖前的預標是 198 個框；使用者審圖後 **103 張有修改、刪掉 13 個框、沒有新增框**，
保留的框與預標的中位 IoU 0.747。47 張與預標完全相同，其中 19 張本來就是無軌跡。

> **新踩到的坑：LabelImg 會覆寫 `classes.txt`。** 審圖後該檔變成 LabelImg 內建的
> 預設清單（dog／person／cat…），`001.txt` 的框也被寫成類別 **15**。
> `check_annotation_return.py` 的類別檢查擋下來了，已修正並在
> `prelabel_assist.py diff` 加了同樣的檢查。**日後任何人用 LabelImg 審圖都要看這個檔。**

### 7.4 兩條限制

- **A 包不可預標。** A 測的是兩人一致性，共同的預標框會把一致性灌高，測試就失去意義。
- **來源要記錄。** ext 臂的 `_provenance.json` 必須寫明「AI 預標 ＋ 單人審」，並保存 `diff` 的輸出。

---

## 8. A 包回收結果（2026-09-12）

**兩位各交 20 張，格式全過；一致性中位 IoU 0.746 < 0.85，v11 §7 的中止條件觸發。**

| | 第一位 | 第二位 |
| --- | --- | --- |
| 標註檔 / 框數 | 20 / 25 | 20 / 27 |
| 類別編號 | 全 0 ✓ | 全 0 ✓ |

兩位都用 **Roboflow** 匯出（不是說明書建議的 X-AnyLabeling／LabelImg，輸出格式相同，可用）。
驗收時額外確認了兩件事，因為 Roboflow 預設會做 auto-orient 與 resize：

- 40 個影像檔與發派出去的 `待標註/` **逐位元相同**（MD5），`README.roboflow.txt` 也註明未做前處理
- Roboflow 在檔名加的雜湊已還原為 `01.txt`…`20.txt`，對照表寫在 `完成後放這裡/_roboflow檔名對照.csv`

完整的結果、失效機制（分歧集中在「分成幾個框」）、以及刪類／保留／改約定重測三條路的代價比較，
見 **[v5.6_結果_工作包A標註一致性.md](v5.6_結果_工作包A標註一致性.md)**。

> §7.4 的「A 包不可預標」已經執行到底：A 包全程沒有任何預標框。
